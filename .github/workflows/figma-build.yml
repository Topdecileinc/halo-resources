# Figma → email block builder (Stage 1).
#
# Triggered by the webhook receiver via repository_dispatch (or run manually for testing).
# Pulls one Figma node via REST, translates it to an email block with Claude Code, runs the
# block validation gate, and commits ONLY if the gate passes. The thin wrapper lives here
# (GitHub requires workflows under .github/workflows/); the real logic is in figma-pipeline/.
#
# DEV TO DO before this runs:
#   - Add repo Actions secrets: ANTHROPIC_AUTH_TOKEN (from `claude setup-token`, uses your
#     Claude subscription), FIGMA_CLIENT_ID, FIGMA_CLIENT_SECRET, FIGMA_REFRESH_TOKEN.
#   - This is UNTESTED here (no runner/secrets) — smoke-test with the manual trigger first.
#   - Confirm the Claude Code install command + flags against current docs at deploy time.

name: figma-build

on:
  repository_dispatch:
    types: [figma-publish]
  workflow_dispatch:        # manual trigger for smoke-testing
    inputs:
      file_key:    { description: "Figma file key", required: true }
      node_id:     { description: "Figma node id (e.g. 4618:108)", required: true }
      output_path: { description: "Block file to (re)generate", required: true }

permissions:
  contents: write           # to commit the regenerated block

concurrency:                # don't let two rebuilds race the same refresh-token/commit
  group: figma-build
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      FIGMA_CLIENT_ID:     ${{ secrets.FIGMA_CLIENT_ID }}
      FIGMA_CLIENT_SECRET: ${{ secrets.FIGMA_CLIENT_SECRET }}
      FIGMA_REFRESH_TOKEN: ${{ secrets.FIGMA_REFRESH_TOKEN }}
      ANTHROPIC_AUTH_TOKEN: ${{ secrets.ANTHROPIC_AUTH_TOKEN }}
      FILE_KEY:    ${{ github.event.client_payload.file_key    || github.event.inputs.file_key }}
      NODE_ID:     ${{ github.event.client_payload.node_id     || github.event.inputs.node_id }}
      OUTPUT_PATH: ${{ github.event.client_payload.output_path || github.event.inputs.output_path }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with: { python-version: "3.x" }

      - name: Pull the node from Figma (REST, headless)
        run: python3 figma-pipeline/figma_fetch.py --file "$FILE_KEY" --node "$NODE_ID" --out ./_figma_in

      - uses: actions/setup-node@v4
        with: { node-version: "20" }

      - name: Install Claude Code
        run: npm install -g @anthropic-ai/claude-code

      - name: Translate to an email block (Claude Code, headless)
        # NOTE on --bare: it gives reproducible CI runs but trims tools to Read/Edit/Bash (no
        # Write). This pipeline REFRESHES existing blocks (Edit is enough), so --bare is safe
        # for refreshes; it's omitted here so Write also works if a target file doesn't exist yet.
        run: |
          claude -p "Regenerate the email block at $OUTPUT_PATH from the Figma design in \
            ./_figma_in/node.json and ./_figma_in/node.png. Follow \
            developer-skills/rules_figma_download.md exactly: translate to email-safe table HTML, \
            keep the [BRACKETED] placeholders, preserve hand edits per the Refresh section, and \
            recompute the figma-source generated-sha. Write ONLY the updated block to \
            $OUTPUT_PATH and its playground; change nothing else." \
            --allowedTools "Read,Edit,Write,Bash(python3 *)" \
            --permission-mode dontAsk \
            --max-turns 12 --max-budget-usd 2.00 \
            --output-format json

      - name: Regenerate the manifest
        run: python3 figma-pipeline/generate_manifest.py

      - name: Validation gate (blocks the commit on failure)
        run: python3 figma-pipeline/validate_block.py "$OUTPUT_PATH"

      - name: Commit on green
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name  "figma-pipeline[bot]"
            git config user.email "figma-pipeline@users.noreply.github.com"
            git add -A
            git commit -m "figma: rebuild ${OUTPUT_PATH} from node ${NODE_ID}"
            git push
          else
            echo "No changes — nothing to commit."
          fi
